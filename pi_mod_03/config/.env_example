# --------------------
# PostgreSQL Settings
# --------------------

# Nombre del host de la DB (solo para conexión TCP)
DB_HOST=127.0.0.1

# Puerto para PostgreSQL (default: 5432)
POSTGRES_PORT=5432

# Nombre de la base de datos
POSTGRES_DB=airbnb_db

# Usuario de la base de datos
POSTGRES_USER=airbnb_user

# Contraseña del usuario
POSTGRES_PASSWORD=your_secure_password_here

# Nombre de la conexión de Cloud SQL (para sockets Unix)
# Formato: [PROJECT_ID]:[REGION]:[INSTANCE_ID]
CONNECTION_NAME=tu-proyecto:us-east1:airbnb-db-instance

# URL de conexión completa (solo necesario si querés sobreescribir DB_URL manualmente)
# Formato para sockets:
# postgresql+psycopg2://<user>:<password>@/<db>?host=/cloudsql/<connection_name>
DB_URL=postgresql+psycopg2://airbnb_user:your_secure_password_here@/airbnb_db?host=/cloudsql/tu-proyecto:us-east1:airbnb-db-instance

# --------------------
# Google Cloud Storage
# --------------------
GCS_BUCKET=pi-mod03
GCS_FILE_PATH=raw/data/airbnb/

# --------------------
# DBT Paths (opcional)
# --------------------
DBT_PROJECT_PATH=/path/to/your/dbt_project
DBT_PROFILES_DIR=/path/to/your/dbt_project/.dbt
